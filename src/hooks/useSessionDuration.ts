/**
 * Track session duration and pages viewed.
 *
 * V10: Removed — Umami tracks session duration natively.
 * Hook kept as no-op for backward compatibility.
 */
export function useSessionDuration(): void {
  // V10: No-op — Umami built-in session tracking is sufficient
}
