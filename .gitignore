# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
coverage/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files
.vscode/
.cursor/
.idea/
*.swp
*.swo
*~

# Claude Code (keep configuration, ignore sensitive data)
CLAUDE.md
.claude
.claude/.env
.claude/cache/
.claude/logs/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Deployment
.vercel/
.netlify/

# Vite cache
.vite/

# TypeScript
*.tsbuildinfo

# Testing
coverage/
.nyc_output
# Compiled test files (Vitest should only run .ts/.tsx sources)
src/**/*.test.js
src/**/*.spec.js
src/__tests__/**/*.js

# Local development data (keep raw/ for development)
raw/

# Design folders (experimental designs)
designs/

# Scripts folders (ignore contents, not the dir — so specific files can be un-ignored)
scripts/*

# Serena cache
.serena

# AI artifacts (separate private repo)
.ai/

# Conclave advisor instance (symlink into the conclave repo)
.conclave

# Local Umami DB backups (never commit — contains analytics data)
umami-backup-*.sql.gz

# Retention script must ship despite the scripts/ ignore above (CI runs it)
!scripts/umami-retention.sql

# design-sync (staged converter, generated bundle, machine state)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
